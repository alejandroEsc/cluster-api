apiVersion: "cluster.k8s.io/v1alpha1"
kind: Cluster
metadata:
  name: $CLUSTER_NAME
spec:
    clusterNetwork:
        pods:
            cidrBlocks: ["192.168.0.0/16"]
        serviceDomain: "maas.cluster.local"
    providerConfig:
      value:
        apiVersion: "maasproviderconfig/v1alpha1"
        kind: "MAASClusterProviderConfig"
